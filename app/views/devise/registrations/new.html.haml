.container 
  .top-container
    %h1.icon 
      = link_to image_tag("/material/logo/logo.png", with: "100", class: 'header__logo'), root_path 

  .main-container
    .main-container__box
      .main-container__box__top
        会員情報入力
      .user-box
        = form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: true do |f|
          = render "devise/shared/error_messages", resource: resource
          .form-box  
            .input-box__nickname
              = f.label :nickname, "ニックネーム", class: 'input-label'
              %span.form-require 
                必須
              %br/ 
              = f.text_field :nickname, class: 'input-default', placeholder: '  フリマ'
            .input-box__email
              = f.label :email, "メールアドレス", class: 'input-label'
              %span.form-require 
                必須
              %br/ 
              = f.email_field :email, class: 'input-default', placeholder: '  PC・携帯電話どちらでも可'
            .input-box__password
              = f.label :encrypted_password, "パスワード", class: 'input-label'
              %span.form-require 
                必須
              %br/ 
              = f.password_field :encrypted_password, class: 'input-default', placeholder: '  7文字以上の半角英数字', id: 'encrypted_password'
            .input-box__password-confirmation
              = f.label :encrypted_password, "パスワード（再入力）", class: 'input-label'
              %span.form-require 
                必須
              %br/ 
              = f.password_field :encrypted_password, class: 'input-default', placeholder: '  7文字以上の半角英数字（再入力）', id: 'encrypted_password'
            .input-box__identification
              .content
                本人確認
                .text
                  安心・安全にご利用いただくために、お客さまの
                  %br/ 
                  本人情報の登録にご協力ください。他のお客さま
                  %br/ 
                  に公開されることはありません。
            .input-box__name
              = f.label :name, "  お名前(全角)", class: 'input-label'
              %span.form-require 
                必須
              %br/ 
              = f.text_field :name, class: 'input-default', placeholder: '  例)山田'
              = f.text_field :name, class: 'input-default', placeholder: '  例)太郎'
            .input-box__furigana
              = f.label :furigana, "  お名前カナ(全角)", class: 'input-label'
              %span.form-require 
                必須
              %br/ 
              = f.text_field :furigana, class: 'input-default', placeholder: '  例)ヤマダ'
              = f.text_field :furigana, class: 'input-default', placeholder: '  例)タロウ'
            .input-box__birthdate
              = f.label :birthdate, "  生年月日", class: 'input-label'
              %span.form-require 
                必須
              %br/
              != sprintf(f.date_select(:birthday, prefix:'birthday',with_css_classes:'birthday', prompt:"--",use_month_numbers:true, start_year:Time.now.year, end_year:1900, date_separator:'%s'),'年','月')+'日'
              .birthdate-text
                ※ 本人情報は正しく入力してください。会員登録
                %br/ 
                後、修正するにはお時間を頂く場合があります。
          .nextpage
            .nextpage__text
              「次へ進む」のボタンを押すことにより、
              = link_to '利用規約'
              に同意したものとみなします
            = f.submit '次へ進む', class: 'red-btn'
          .recaptcha
            .recaptcha-text
              This site is protected by reCAPTCHA and the Google 
              = link_to 'Privacy Policy', class: 'Privacy Policy'
              and
              = link_to 'Terms of Service', class: 'Terms of Service'
              apply.
  .footer
    %ul
      %li 
        = link_to 'プライバシーポリシー' 
      %li
        = link_to 'FURIMA' 
      %br/ 
      %li
        = link_to '特定商取引に関する表記' 
    .footer__logo
      = link_to image_tag("/material/logo/logo-white.png", with: "100", class: "logo-white"), root_path 
      %p
        © FURIMA
        